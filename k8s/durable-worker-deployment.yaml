apiVersion: apps/v1
kind: Deployment
metadata:
  name: gradewise-durable-worker
  labels:
    app: gradewise-durable-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gradewise-durable-worker
  template:
    metadata:
      labels:
        app: gradewise-durable-worker
    spec:
      containers:
        - name: gradewise-durable-worker
          image: gradewise-durable-worker:latest
          ports:
            - name: web
              containerPort: 8080
          resources:
            limits:
              cpu: "1"
              memory: "512Mi"
            requests:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: gradewise-durable-worker
spec:
  selector:
    app: gradewise-durable-worker
  ports:
    - name: worker
      port: 8080
      targetPort: web
