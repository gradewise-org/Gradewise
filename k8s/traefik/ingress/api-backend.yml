apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: api-backend-ingress }
spec:
  entryPoints: [web, websecure]
  tls: {}
  routes:
    - match: Host(`dev.gradewise.org`) && PathPrefix(`/api`)
      kind: Rule
      services: [{ name: gradewise-api-backend, port: web }]
