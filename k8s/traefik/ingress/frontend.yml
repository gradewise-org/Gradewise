apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: frontend-ingress }
spec:
  entryPoints: [web]
  routes:
    - match: Host(`dev.gradewise.org`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: gradewise-frontend
          port: web
